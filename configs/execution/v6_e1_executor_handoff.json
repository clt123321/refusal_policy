{
  "handoff_id": "V6-E1-DIRECT-HANDOFF-002",
  "base_sha": "90cf38bb632c0e6ca861596a59dec4acba23f8ba",
  "source_relay": {"branch": "refusal-policy-handoff-4090-20260915-d8bbb01", "sha": "77ed1780f56affc6ee6af5e73b20f2cce22cefba"},
  "task_plan": "configs/execution/v6_e1_plan.json",
  "execution_sha_policy": "record the exact clean local commit for each run; DEV need not wait for GitHub publication",
  "model": {"repo_id": "Qwen/Qwen2.5-1.5B-Instruct", "revision": "989aa7980e4cf806f80c7fef2b1adb7bc71aa306", "dtype": "float16"},
  "datasets": ["D_A0_loc", "D_repair", "D_endpoint_match", "D_endpoint_audit", "D_A1_train", "D_attack_eval", "D_function", "D_plasticity"],
  "executor_actions": [
    "pin and hash each dataset split (file_sha256, ids_sha256 per V4_PROTOCOL_SCHEMA)",
    "run A1 dynamic-range pilot with ONE shared configuration across B/C/P; avoid floor/ceiling; freeze numeric grid",
    "run content-level evaluator + human-audit aggregate at boundaries; retain raw outputs in restricted store only",
    "construct C=repair(Base) and P=attack(Base)->repair with identical frozen recipe; save A0 provenance checkpoint",
    "qualify endpoints on D_endpoint_match, confirm on sealed D_endpoint_audit before any A1 outcome",
    "run fresh A1 (new adapter, reset optim/sched/scaler/RNG, no A0 artifact reuse) on all qualified endpoints",
    "run frozen benign novel-task plasticity control on B/C/P"
  ],
  "required_manifest_fields": ["repo_commit", "environment", "model", "data", "config_sha256", "seed", "hardware", "cost", "status", "artifacts"],
  "validation_procedure": [
    "schema-validate each run manifest against research/protocols/V4_PROTOCOL_SCHEMA.json",
    "schema-validate result cards against V4_RESULT_SCHEMA.json",
    "verify dataset dedup: full exact dedup + family separation across all splits",
    "verify deterministic replay of cost ledger ordering (logical trial id, not completion order)",
    "verify no sealed attack outcome reached endpoint selection code"
  ],
  "return_to_agent": ["aggregate/minimal per-sample fields only", "content hashes", "schema-validated result cards"],
  "raw_output_policy": "restricted store only; never committed; never relayed",
  "capability_assignment": {
    "Luna": ["benign DEV repair", "asset preparation", "CPU validation", "manifests"],
    "authorized_A0_executor": ["A0 intervention and artifact"],
    "authorized_A1_executor": ["fresh A1 parameter attack"],
    "authorized_content_evaluator": ["content-level evaluation and restricted raw outputs"]
  },
  "terminal_rule": "MATCHING_FAILED is a completed audit: save receipts, skip A1/plasticity, emit INVALID",
  "readiness_rule": "a blocker affects only descendants; continue all other ready tasks; no continuous queue",
  "status": "PARTIAL_EXECUTION_CAPABILITY_REPAIR_DEV_NEXT"
}
